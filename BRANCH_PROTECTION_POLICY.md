# Branch Protection — Quick Policy
- Require PR before merge
- Require status checks (CI) to pass
- Require conversation resolution
- (Optional) signed commits, linear history
- Enable auto‑merge after checks
